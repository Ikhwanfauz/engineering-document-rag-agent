# Optional local runtime overrides
# These values must be set in the process environment.
# The Python application does not automatically load a .env file.

# Ollama service used by the FastAPI backend
OLLAMA_BASE_URL=http://localhost:11434

# FastAPI service used by the Streamlit dashboard
ENGDOC_API_URL=http://127.0.0.1:8000

# SQLite interaction and feedback database
DATABASE_PATH=database/engineering_document_ai.db
